# *** imports

# ** core
from typing import Any

# *** utils

def format_result(value: Any) -> str:
    """Format a value — but this def has no # ** section header above it."""

    # Return string.
    return str(value)
